import { Region } from '../types/region';

export class Regions {
    public static APAC: Region = {
        endpoint: 'ap',
    };

    public static BR: Region = {
        endpoint: 'br',
    };

    public static EU: Region = {
        endpoint: 'eu',
    };

    public static KR: Region = {
        endpoint: 'kr',
    };

    public static LATAM: Region = {
        endpoint: 'latam',
    };

    public static NA: Region = {
        endpoint: 'na',
    };

    public static PBE1: Region = {
        endpoint: 'pbe1',
    };

    /**
     * Regions for the Account API
     */
    public static ASIA: Region = {
        endpoint: 'asia',
    };

    public static AMERICAS: Region = {
        endpoint: 'americas',
    };

    public static EUROPE: Region = {
        endpoint: 'europe',
    };
}
